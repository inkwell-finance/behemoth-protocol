/**
 * gRPC type exports for the Behemoth trading network.
 *
 * Import from `@inkwell-finance/behemoth-protocol/grpc`:
 *
 *   import type { BacktestRequest, BacktestResponse } from '@inkwell-finance/behemoth-protocol/grpc';
 *
 * These types are generated from src/grpc/trader.proto.
 * Run `npm run proto:gen` in the protocol package to regenerate.
 */
export * from "./generated/trader";
